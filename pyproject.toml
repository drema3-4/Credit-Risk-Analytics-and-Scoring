[project]
name = "credit_risk_analytics_and_scoring"
version = "0.1.0"
description = ""
authors = [
    {name = "Drema"}
]
requires-python = "==3.12.12"
dependencies = [
    "pandas (>=3.0.5,<4.0.0)",
    "matplotlib (>=3.11.1,<4.0.0)",
    "seaborn (>=0.13.2,<0.14.0)",
    "ipykernel (>=7.3.0,<8.0.0)",
    "jupyter (>=1.1.1,<2.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
